# SQL-COUNT-DISTINCT-and-LIMIT-Lab
In this lab project, I retrieve the number of rows that match a query criteria, remove duplicate values from a result set and return the unique values, and restrict the number of rows retrieved from a table.
